# تنظیمات ساده پروژه

برای اتصال پروژه به GitHub:
1. در `public-site/src/site-config.js` نام کاربری، نام repository و branch را وارد کنید.
2. همان اطلاعات را در `admin-site/src/site-config.js` و `admin-site/api/repo-config.mjs` وارد کنید.
3. فقط `GITHUB_TOKEN` را در Environment Variables پروژه Admin در Vercel قرار دهید.

Token را در فایل‌های پروژه یا GitHub ذخیره نکنید.

# Content

`menu.json` منبع اصلی اطلاعات منو است.

`images/` محل ذخیرهٔ فایل‌های تصویری آیتم‌هاست. پنل ادمین هنگام آپلود، نام فایل را یکتا می‌کند و تصویر را قبل از ارسال فشرده می‌کند.

برای ویرایش دستی، ساختار `menu.json` را حفظ کن.
